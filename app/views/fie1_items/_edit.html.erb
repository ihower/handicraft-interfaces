<%# remote_form_for item , :url => fie1_item_path(item) , :html => {:method => :put} do |f| -%>
<% form_for item , :url => fie1_item_path(item) , :html => { :method => :put, :class => 'h-put' } do |f| -%>

  <%= f.text_field :name %>
  
  <%= f.submit "Submit", :class => 'submit' %> or <%= cancel_link( 'cancel', item) %>  
  
<% end %>

